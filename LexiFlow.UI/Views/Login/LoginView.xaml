<Window x:Class="LexiFlow.UI.Views.Login.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LexiFlow.UI.Views.Login"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="{DynamicResource Login_Title}" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        TextElement.Foreground="{DynamicResource TextBrush}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <Style x:Key="LanguageButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" CornerRadius="20">
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="{DynamicResource SecondaryBrush}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource SecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left side - Decorative area -->
        <Grid Grid.Column="0" Background="{DynamicResource PrimaryBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- App logo and name -->
            <StackPanel Grid.Row="0" Margin="30,40,0,0">
                <TextBlock Text="LexiFlow" FontSize="36" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="日本語を学ぼう" FontSize="20" Margin="0,5,0,0" Foreground="White" Opacity="0.8"/>
            </StackPanel>

            <!-- Center graphic -->
            <Image Grid.Row="1" Source="/Resources/Images/japanese_learning.png" 
                   Stretch="Uniform" Margin="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <!-- Footer text -->
            <TextBlock Grid.Row="2" Text="© 2025 LexiFlow. All rights reserved." 
                       Foreground="White" Opacity="0.6" Margin="30,0,0,20"/>
        </Grid>

        <!-- Right side - Login form -->
        <Grid Grid.Column="1">
            <!-- Language selection in top-right corner -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,20,0">
                <Button x:Name="btnVN" Style="{StaticResource LanguageButtonStyle}" 
                        Command="{Binding ChangeLanguageCommand}" CommandParameter="VN"
                        ToolTip="{DynamicResource Language_Vietnamese}">
                    <TextBlock Text="VN" FontWeight="Bold"/>
                </Button>
                <Button x:Name="btnEN" Style="{StaticResource LanguageButtonStyle}" 
                        Command="{Binding ChangeLanguageCommand}" CommandParameter="EN"
                        ToolTip="{DynamicResource Language_English}">
                    <TextBlock Text="EN" FontWeight="Bold"/>
                </Button>
                <Button x:Name="btnJP" Style="{StaticResource LanguageButtonStyle}" 
                        Command="{Binding ChangeLanguageCommand}" CommandParameter="JP"
                        ToolTip="{DynamicResource Language_Japanese}">
                    <TextBlock Text="JP" FontWeight="Bold"/>
                </Button>
            </StackPanel>

            <!-- Login form -->
            <Border Width="300" Height="400" CornerRadius="10" Background="White" 
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    materialDesign:ShadowAssist.ShadowDepth="Depth2">
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Login header -->
                    <TextBlock Grid.Row="0" Text="{DynamicResource Login_Header}" 
                               FontSize="22" FontWeight="Bold" Margin="0,0,0,20"
                               HorizontalAlignment="Center"/>

                    <!-- Username field -->
                    <TextBox Grid.Row="1" 
                             materialDesign:HintAssist.Hint="{DynamicResource Login_Username}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,10,0,10">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding LoginCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <!-- Password field -->
                    <PasswordBox Grid.Row="2" x:Name="PasswordBox"
                                 materialDesign:HintAssist.Hint="{DynamicResource Login_Password}"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 Margin="0,10,0,10"
                                 PasswordChanged="PasswordBox_PasswordChanged">
                        <PasswordBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding LoginCommand}"/>
                        </PasswordBox.InputBindings>
                    </PasswordBox>

                    <!-- Remember me checkbox -->
                    <CheckBox Grid.Row="3" Content="{DynamicResource Login_RememberMe}" 
                              IsChecked="{Binding RememberMe}"
                              Margin="0,10,0,10"/>

                    <!-- Error message -->
                    <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" 
                               Foreground="Red" TextWrapping="Wrap"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                               Margin="0,5,0,5"/>

                    <!-- Login button -->
                    <Button Grid.Row="5" Content="{DynamicResource Login_Button}" 
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource PrimaryBrush}"
                            Height="45" Margin="0,20,0,10"/>

                    <!-- Forgot password link -->
                    <TextBlock Grid.Row="6" Text="{DynamicResource Login_ForgotPassword}"
                               Foreground="{DynamicResource SecondaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Loading indicator -->
            <Grid Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" 
                             Value="0" IsIndeterminate="True" Width="50" Height="50"/>
            </Grid>
        </Grid>
    </Grid>
</Window>