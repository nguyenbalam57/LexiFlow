<UserControl x:Class="LexiFlow.AdminDashboard.Views.MediaManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:LexiFlow.AdminDashboard.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="16">
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Media Management" 
                          Style="{StaticResource SectionHeaderStyle}"
                          FontSize="24" FontWeight="Bold"/>
                
                <TextBlock Text="Manage audio files, images, and multimedia content"
                          Margin="0,0,0,20"
                          Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Toolbar -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="{DynamicResource PrimaryHueMidBrush}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Upload" Margin="0,0,4,0"/>
                                <TextBlock Text="Upload Media"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderMultiple" Margin="0,0,4,0"/>
                                <TextBlock Text="Categories"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" Margin="0,0,4,0"/>
                                <TextBlock Text="Settings"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <!-- Content placeholder -->
                    <Border Grid.Row="1" 
                            Background="{DynamicResource MaterialDesignDivider}"
                            CornerRadius="4" 
                            Height="400">
                        <TextBlock Text="Media management interface will be implemented here"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  FontSize="16"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>